
  # GOGLOBAL (Community)

  This is a code bundle for GOGLOBAL (Community). The original project is available at https://www.figma.com/design/uOm36uogQMYNvLpCqhPfpq/GOGLOBAL--Community-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  